# Cópia local do workflow que roda no vesti-mobi/dados.
# No repositório ele vive em .github/workflows/painel-clientes-cs.yml
#
# Roda todo dia às 04:00 BRT (07:00 UTC), refaz o CS/dados.js inteiro
# (BigQuery + HubSpot) e commita, junto com o retrato de integrações
# (CS/integracoes_snapshot.json + CS/integracoes_novas.json). Esses dois
# precisam voltar para o repositório: é a comparação com o retrato da carga
# anterior que detecta integração nova, e sem commitar o retrato nasceria vazio
# toda vez. Todas as abas, tabelas e gráficos do painel
# saem desse único arquivo, então uma carga atualiza o painel inteiro.
#
# Secrets usados (todos já existem no repo): GCP_SA_KEY, HUBSPOT_TOKEN e
# RELCS2_TINO_USER / RELCS2_TINO_PASS — a credencial do painel do Tino, a mesma
# que o relatoriocs2 usa. Sem ela a carga roda igual, só que a aba do Tino e a
# coluna "Tino" da tabela geral ficam vazias.
#
# Antes da carga, sincronizar_cs.js corrige o CS responsável (angel_id) de
# odbc_domains no BigQuery comparando com o Postgres de produção via Metabase
# (secrets METABASE_URL, METABASE_API_KEY) — achado em 04/09/2026: a troca de
# CS no admin da Vesti às vezes não atualiza o updated_at da linha, e a
# réplica pro BigQuery usa esse campo pra saber o que sincronizar, então a
# troca fica invisível pra sempre. Sem esses secrets, ou se o Metabase cair,
# o passo avisa e segue sem travar a carga — o espelho fica como estava.
#
# ingerir_stock_logs.js (pedido da Laura, 14/09/2026) espelha public.stock_logs
# (log de movimentação de estoque) pra vestilake_BI.postgres_stock_logs, mesmo
# mecanismo Metabase -> BigQuery. Roda todo dia, sozinho. A origem tinha só
# 15/08-21/08/2026 (250.547 linhas) e não recebeu nenhuma linha nova depois
# disso (confirmado com now() do próprio banco em 14/09/2026) — enquanto isso
# não mudar, este espelho é a única cópia com esses dados. Não tem relação com
# o painel de CS em si; mora aqui só por reaproveitar os secrets e o
# agendamento diário já prontos. Mesma resiliência: falha e segue sem travar
# a carga do painel.

name: Painel de Clientes CS

on:
  schedule:
    - cron: '0 7 * * *'   # 04:00 BRT
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # o fetch_dados.js resolve o @google-cloud/bigquery na RAIZ do repo
      - name: Instalar dependencias Node
        run: npm install --no-audit --no-fund

      - name: Gravar credencial BigQuery (SA key)
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          printf '%s' "$GCP_SA_KEY" > "$RUNNER_TEMP/gcp_sa.json"
          echo "GOOGLE_APPLICATION_CREDENTIALS=$RUNNER_TEMP/gcp_sa.json" >> "$GITHUB_ENV"

      - name: Sincronizar CS responsável (Metabase -> BigQuery)
        working-directory: CS
        env:
          METABASE_URL: ${{ secrets.METABASE_URL }}
          METABASE_API_KEY: ${{ secrets.METABASE_API_KEY }}
        run: node sincronizar_cs.js

      - name: Ingerir stock_logs (Metabase -> BigQuery)
        working-directory: CS
        env:
          METABASE_URL: ${{ secrets.METABASE_URL }}
          METABASE_API_KEY: ${{ secrets.METABASE_API_KEY }}
        run: node ingerir_stock_logs.js

      - name: Rodar carga (BigQuery + HubSpot + Tino)
        working-directory: CS
        env:
          HUBSPOT_TOKEN: ${{ secrets.HUBSPOT_TOKEN }}
          TINO_USER: ${{ secrets.RELCS2_TINO_USER }}
          TINO_PASS: ${{ secrets.RELCS2_TINO_PASS }}
        run: node fetch_dados.js

      # trava de sanidade: dados.js tem ~6 MB. Se vier um arquivo minúsculo,
      # alguma fonte falhou e é melhor não publicar por cima do que está no ar.
      - name: Conferir o tamanho do dados.js
        run: |
          BYTES=$(stat -c%s CS/dados.js)
          echo "dados.js: $((BYTES/1048576)) MB"
          if [ "$BYTES" -lt 1000000 ]; then
            echo "dados.js veio pequeno demais ($BYTES bytes) — abortando sem commit"
            exit 1
          fi

      - name: Commit e push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          MSG="Painel de Clientes CS: dados de $(TZ=America/Sao_Paulo date +'%d/%m/%Y %H:%M')"
          ARQS="dados.js integracoes_snapshot.json integracoes_novas.json"
          for a in $ARQS; do if [ -f "CS/$a" ]; then cp "CS/$a" "$RUNNER_TEMP/$a"; fi; done
          for i in 1 2 3 4 5; do
            git fetch -q origin main
            git reset -q --hard origin/main
            for a in $ARQS; do if [ -f "$RUNNER_TEMP/$a" ]; then cp "$RUNNER_TEMP/$a" "CS/$a"; fi; done
            for a in $ARQS; do if [ -f "CS/$a" ]; then git add "CS/$a"; fi; done
            if git diff --cached --quiet; then echo "sem mudancas"; exit 0; fi
            git commit -q -m "$MSG"
            if git push; then echo "push ok (tentativa $i)"; exit 0; fi
            echo "push falhou (tentativa $i), re-sincronizando..."
            sleep 3
          done
          echo "push falhou apos varias tentativas"; exit 1
